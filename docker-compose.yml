services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./output:/app/output
      - ./cache:/app/cache
      - ${CONFIG_FILE:-./config.toml.example}:/app/config.toml:ro
    environment:
      - DISCOGS_TOKEN=${DISCOGS_TOKEN:-}
