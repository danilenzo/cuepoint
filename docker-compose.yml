services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./output:/app/output
      - ./cache:/app/cache
      - ./config.toml:/app/config.toml:ro
    environment:
      - DISCOGS_TOKEN=${DISCOGS_TOKEN:-}
