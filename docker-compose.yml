services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./output:/app/output
      - ./lib/parser/cache:/app/lib/parser/cache
    environment:
      - DISCOGS_TOKEN=${DISCOGS_TOKEN:-}
